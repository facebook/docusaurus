---
slug: /migration
---

# 升级 Docusaurus

Docusaurus 的版本号是基于 `major.minor.patch` 方案，并遵从[**语义化版本**](https://semver.org/lang/zh-CN/)规范。

**破坏性更改**只在主版本号（major）升级时发布，并在以下升级指南中详细记录。

import DocCardList from '@theme/DocCardList';

<DocCardList />
