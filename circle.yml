machine:
  node:
    version: 6.11.2
  npm:
    version: 3.10.10

test:
  override:
    - "true"

deployment:
  website:
    branch: master
    commands:
      - git config --global user.email "facebook-github-bot@users.noreply.github.com"
      - git config --global user.name "Docusaurus Documentation Deploy Script"
      - npm install && cd website && GIT_USER=facebook-github-bot npm run publish-gh-pages
