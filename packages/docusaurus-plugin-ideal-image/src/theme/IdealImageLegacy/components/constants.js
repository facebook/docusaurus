const load = 'load';
const loading = 'loading';
const loaded = 'loaded';
const error = 'error';
const noicon = 'noicon';
const offline = 'offline';

export const icons = {
  load,
  loading,
  loaded,
  error,
  noicon,
  offline,
};

const initial = 'initial';

export const loadStates = {
  initial,
  loading,
  loaded,
  error,
};
